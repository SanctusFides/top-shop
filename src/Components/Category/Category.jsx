import Nbar from '../Nbar/Nbar';

export default function Category({cart}) {
  // const categoryURL = "";
  // console.log(category);

  // const { isLoading, data, error } = useFetch(
  //   "https://fakestoreapi.com/products/category/${category}"
  // );
  // const cartCopy = [...cart]
  console.log(cart);

  
  return (
    <h1>Landed</h1>
  )
}
